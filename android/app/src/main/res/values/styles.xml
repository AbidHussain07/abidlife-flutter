<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme applied to the MainActivity while the OS shows the system splash
         (before Flutter has rendered its first frame). -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <!-- Theme applied as soon as the Flutter UI is rendered. Inherits from
         a transparent Material theme so Flutter controls all chrome. -->
    <style name="NormalTheme" parent="Theme.Material3.DynamicColors.Light">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
